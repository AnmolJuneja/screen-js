import { PlaylistInsertDirective } from './playlist-insert.directive';

describe('PlaylistInsertDirective', () => {
  it('should create an instance', () => {
    const directive = new PlaylistInsertDirective(null);
    expect(directive).toBeTruthy();
  });
});
